import React from 'react';

/**
 * OrderDetail Component
 * This page displays the details of a specific order for the user.
 */
const OrderDetail = () => {
  return (
    // Main container with enough padding to match the clean layout
    <div style={{ padding: '40px', fontFamily: "'Inter', sans-serif", backgroundColor: '#f0f7ff', minHeight: '100vh' }}>
      
      <h1 style={{ 
        fontSize: '36px', 
        fontWeight: '700', 
        color: '#000000', 
        marginBottom: '32px' 
      }}>
        Order Detail  
      </h1>

    </div>
  );
};

export default OrderDetail;